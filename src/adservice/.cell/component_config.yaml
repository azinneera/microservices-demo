apiVersion: v1alpha1
kind: ComponentConfig
metadata:
  name: adservice
  projectName: gcp-demo
  annotations:
    componentType: service
spec:
  inbound:
  - name: adservice
    port: 9095
    type: REST
    visibility: Organization
    context: /
