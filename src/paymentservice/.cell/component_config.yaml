apiVersion: v1alpha1
kind: ComponentConfig
metadata:
  name: paymentservice
  projectName: gcp-demo
  annotations:
    componentType: service
spec:
  inbound:
  - name: paymentservice
    port: 9095
    type: REST
    visibility: Organization
    context: /
